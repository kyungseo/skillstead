> **Latest** refers to the most recently published individual skill release, not a catalog version.

## docs-claim-check 0.8.0

This baseline release establishes the per-skill tag `docs-claim-check/v0.8.0`. It carries forward the frozen
`0.8.0` package bytes and does not change the skill's contract or runtime support.

Use the namespaced tag for reproducible installation. See
[`docs/INSTALL.md`](https://github.com/kyungseo/skillstead/blob/main/docs/INSTALL.md) for the supported paths and
clean-update procedure.
