> **Latest** refers to the most recently published individual skill release, not a catalog version.

## writing-quality-editor 0.8.0

This baseline release establishes the per-skill tag `writing-quality-editor/v0.8.0`. It carries forward the frozen
`0.8.0` package bytes and does not change the skill's preservation-first writing contract or localization scope.

Use the namespaced tag for reproducible installation. See
[`docs/INSTALL.md`](https://github.com/kyungseo/skillstead/blob/main/docs/INSTALL.md) for supported runtime paths
and clean updates.
