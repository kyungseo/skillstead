> **Latest** refers to the most recently published individual skill release, not a catalog version.

## github-release-guide 0.8.0

This baseline release establishes the per-skill tag `github-release-guide/v0.8.0`. It carries forward the frozen
`0.8.0` package bytes and does not change the skill's contract or recorded support boundary.

The repository installation examples now pin this namespaced tag. See
[`docs/INSTALL.md`](https://github.com/kyungseo/skillstead/blob/main/docs/INSTALL.md) for global and project paths
on macOS, Linux, and Windows.
