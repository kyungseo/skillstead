# Codex Fresh-Context Raw Outputs — 2026-07-18

Each scenario ran in a separate context created with no inherited conversation turns. The evaluator could read
only the installed skill folder and received the scenario inline. It was instructed not to inspect examples or
the answer key. Model and reasoning effort were not exposed by the runtime.

## F03 — First Run

## Adapted Text

Acme Notes 2.4에 오프라인 초안 복구 기능이 추가되었습니다. 초안이 동기화되기 전에 앱이 종료된 경우, 같은 노트를 다시 열고 `Restore draft`를 선택하세요.

복구 기능은 macOS 15에서 검증되었습니다. Windows와 Linux에서는 아직 검증되지 않았습니다.

## Preservation Notes

- 제품명 `Acme Notes`와 버전 `2.4`를 그대로 유지했습니다.
- UI 항목 `Restore draft`는 실제 표시 문구일 수 있어 번역하지 않았습니다.
- 복구 조건과 절차를 유지했습니다: 동기화 전 앱 종료 → 같은 노트 다시 열기 → `Restore draft` 선택.
- 검증 범위를 정확히 보존했습니다: macOS 15는 검증 완료, Windows와 Linux는 미검증.
- 한국 사용자를 대상으로 직접적이고 전문적인 릴리스 노트 문체를 적용했습니다.

## Material Changes

- `offline draft recovery`를 자연스러운 한국어 제품 용어인 “오프라인 초안 복구 기능”으로 옮겼습니다.
- 사용자가 변경 사항과 복구 방법을 빠르게 파악하도록 기능 소개, 복구 절차, 검증 범위 순으로 문단을 구성했습니다.
- `remain unverified`의 불확실성을 강화하거나 약화하지 않고 “아직 검증되지 않았습니다”로 명시했습니다.

## F04 — First Run

## Adapted Text

Before the release:

1. Run `git diff --check` to verify that the documentation has no formatting errors.
2. If the check passes, create the `v2.4.0` tag on the current commit of `main`.
3. If the check fails, do not create the tag. Fix the errors, then restart the verification process from the beginning.

## Preservation Notes

- Preserved the protected identifiers exactly: `v2.4.0`, `git diff --check`, and `main`.
- Retained the strict condition that the tag may be created only after the check passes.
- Retained the failure path: do not create the tag, fix the errors, and repeat the full verification process.
- Kept the direct, procedural voice appropriate for an English-language maintainer runbook.

## Material Changes

- Reorganized the source paragraph into ordered steps so the success and failure branches are immediately visible.
- Rendered “문서 형식 오류” as “documentation formatting errors” for natural maintainer-facing English.
- Made “처음부터 다시 확인한다” explicit as restarting the verification process from the beginning, without changing the required action.

## F08 — First Run

## Revised Text

In Acme Export 1.2, the export flow passed manual smoke testing on macOS 15. Windows and Linux were not tested for this release.

## Preservation Notes

- Preserved the product name, version, and export-flow scope.
- Used a concise, factual release-note tone.
- Limited the compatibility claim to the supplied evidence.

## Material Changes

- Replaced “works everywhere” and “fully reliable” with the observed macOS 15 test result.
- Added the Windows and Linux testing limitation.
- Removed the unsupported claim of a seamless experience for every team.

## F09 — First Run

## Adapted Text

> 임시 문안 — 공유 사본에 미치는 영향이 확인되기 전에는 확정할 수 없습니다.
>
> **제목:** 작업 공간 비우기  
> **본문:** 로컬 항목이 삭제되며, 공유 사본에도 영향을 줄 수 있습니다.  
> **기본 버튼:** 비우기  
> **보조 버튼:** 취소

## Preservation Notes

- 로컬 항목이 제거된다는 파괴적 결과를 유지했습니다.
- 공유 사본에 영향이 있을 수 있다는 경고와 불확실성을 유지했습니다.
- 확인과 취소라는 원래의 선택 구조를 보존했습니다.
- `ko-KR` 앱 UI에 맞춰 제목과 버튼을 간결하게 표현했습니다.

## Needs Human

확인이 필요한 원문 범위:

> “may affect shared copies”

공유 사본에 실제로 어떤 일이 일어나는지 제품 동작을 확인해야 합니다. 확인 결과에 따라 다음 중 하나로 확정하는 것이 안전합니다.

1. 공유 사본도 삭제되는 경우  
   **본문:** 로컬 항목과 공유 사본이 삭제됩니다.  
   **기본 버튼:** 모두 삭제  
   가장 강한 파괴적 결과를 명확히 알립니다.
2. 공유 사본과의 연결만 해제되는 경우  
   **본문:** 로컬 항목이 삭제되고 공유 사본과의 연결이 해제됩니다.  
   **기본 버튼:** 삭제 및 연결 해제  
   데이터 삭제와 연결 해제를 구분합니다.
3. 공유 사본은 변경되지 않는 경우  
   **본문:** 로컬 항목만 삭제됩니다. 공유 사본은 변경되지 않습니다.  
   **기본 버튼:** 로컬 항목 삭제  
   작업 범위를 로컬 항목으로 한정합니다.

## Material Changes

- 영어 문장 구조를 그대로 옮기지 않고, 파괴적 결과가 먼저 드러나도록 자연스러운 한국어 문장으로 바꿨습니다.
- `Clear`는 임시로 `비우기`로 옮겼지만, 최종 버튼 문구는 공유 사본의 실제 처리 방식에 맞춰 더 구체적으로 확정해야 합니다.

## F11 — First Run

## Revised Text

Trace one request path across three services, one queue, and one data store. This architecture visualization makes each component and handoff easy to follow.

## Preservation Notes

- Retains the artifact’s identity as an architecture visualization.
- Preserves the demonstrated request path and the exact component counts.
- Keeps the focus on helping developers understand the example.

## Material Changes

- Leads with what the image actually demonstrates.
- Replaces repetitive, promotional phrasing with concise gallery copy.
- Removes unsupported claims about scalability, seamlessness, and innovation.

## F12 — First Run, Pre-Fix

## Revised Text

Acme Diff compares two configuration files and highlights the values that changed.

```sh
acme-diff before.yml after.yml
```

The report is generated locally. Neither file is uploaded.

## Preservation Notes

- Preserved the exact command and filenames.
- Retained the claims that Acme Diff compares two configuration files, identifies changed values, generates a local report, and uploads neither file.
- Kept the tone concise and direct for a developer-facing README.

## Material Changes

- Moved the command into a code block to make the shortest success path easier to scan and copy.
- Separated the local-processing and no-upload boundary from the usage instruction for clearer emphasis.

## F12 — Reproducibility Rerun 1, Pre-Fix

## Revised Text

Acme Diff compares two configuration files and highlights changed values. Run `acme-diff before.yml after.yml` to print a local report. Neither file is uploaded.

## Preservation Notes

- Preserved the product name and command exactly.
- Retained the comparison behavior, changed-value highlighting, local report, and no-upload guarantee.
- Kept the concise, direct tone appropriate for a developer-facing README.

## Material Changes

- Tightened “values that changed” to “changed values.”
- Made the local-only boundary more direct without changing its meaning.

## F12 — Post-Fix Run

## Revised Text

Acme Diff compares two configuration files and highlights values that changed. Run `acme-diff before.yml after.yml` to print a local report; the command does not upload either file.

## Preservation Notes

The source is already concise, natural, and well suited to a developer-facing README. It clearly states the tool’s purpose, provides the shortest usage path, and preserves the local-only processing boundary. The product name and command remain exact.

## Material Changes

None.

## F13 — First Run

## Adapted Text

*Provisional—assign the roles in brackets before publishing this runbook.*

1. Once the review is complete, **[approval requester]** sends the approval request to **[approver]**.
2. Only after **[approver]** approves the request may **[deployment owner]** apply `release-plan.md` to the production repository and proceed with deployment.
3. If an issue occurs after deployment, **[rollback owner]** rolls back to the previous version. **[record owner]** then records the outcome.

## Preservation Notes

- Review still precedes the approval request.
- Approval remains a mandatory prerequisite for updating the production repository and deploying.
- The protected identifier `release-plan.md` is unchanged.
- The rollback condition remains limited to issues that occur after deployment.
- Rollback to the previous version and recording the outcome are both retained.
- No owner or approver was inferred from the source.

## Needs Human

The following source spans cross approval, deployment, rollback, and audit boundaries without assigning actors:

- “검토를 마친 뒤 승인 요청서를 공유한다. 승인을 받은 경우에만 … 반영하고 배포한다.”
- “배포 후 문제가 발생하면 … 되돌리고 결과를 기록한다.”

Choose and document one ownership model:

1. **Single operational owner:** A release maintainer requests approval, deploys, rolls back, and records the outcome; a separate approver grants approval. This minimizes handoffs but concentrates operational responsibility.
2. **Coordinator and operator split:** A release coordinator requests approval and records the outcome, while a deployment operator deploys and rolls back; a named approver grants approval. This clarifies separation of duties but adds a handoff.
3. **Action-specific owners:** Assign a distinct named role to every bracketed action. This provides the strongest audit trail but requires the most process detail.

The runbook should not be finalized until the approval requester, approver, deployment owner, rollback owner, and record owner are identified.

## Material Changes

- Recast the paragraph as ordered runbook steps.
- Made the approval gate explicit with “only after.”
- Separated rollback from outcome recording so each action can have its own owner.
- Added visible role placeholders instead of hiding unresolved ownership behind passive voice or generic imperatives.

## F14 — First Run

## Adapted Text

- You must back up `config.yml` before making changes.
- Running the full test suite is recommended.
- You may attach `review.log`, generated during the review, if needed.
- Do not deploy if a required check fails.

## Preservation Notes

- Preserved the distinct obligation levels: backup is mandatory, full testing is recommended, attaching the log is optional, and deployment is prohibited after a required-check failure.
- Preserved the protected identifiers `config.yml` and `review.log` exactly.
- Used direct runbook language appropriate for English-speaking release maintainers without assigning ownership beyond the stated audience.

## Material Changes

- Converted Korean nominalized constructions into concise English instructions.
- Presented the guidance as an operational checklist while retaining the source order and meaning.

## F15 — First Run

## Revised Text

### Resume an interrupted upload

To continue an interrupted upload, run:

```sh
acme sync --resume
```

You can resume uploads for files up to 5 GB. Upload progress is stored locally. If authentication has expired, the command stops and asks you to sign in again.

## Preservation Notes

- Preserved the interrupted-upload resume capability.
- Kept the protected identifier `acme sync --resume` unchanged.
- Retained the 5 GB file-size limit, local progress storage, and expired-authentication behavior without adding unsupported guarantees.

## Material Changes

- Reordered the content to present the action before support details.
- Added the supplied size limit, progress-storage location, and authentication recovery path.
- Formatted the command as a README-friendly code block.
