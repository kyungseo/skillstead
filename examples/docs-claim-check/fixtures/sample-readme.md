# AcmeTask CLI (synthetic fixture)

> This document is a **synthetic fixture** for docs-claim-check contract testing.
> "AcmeTask" follows the Acme fictitious-product convention — it is not a real
> product, and the npm package name below is deliberately suffixed `-fixture`.

AcmeTask is the fastest task runner in its class.

## Requirements

- Requires Node.js 18 or newer.
- Works on Windows, macOS, and Linux.

## Install

```bash
npm install -g acmetask-fixture
```

Installs in under a minute and runs fully offline after the first run.

## Status

- Latest release: **v2.3.0** (November 2025).
- AcmeTask has zero runtime dependencies.
- All commands are covered by integration tests.
- MIT licensed.
