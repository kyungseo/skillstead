schema_version: 1
typepacks:
  - id: fixture-topology
    selection_signal: "system components and boundaries"
    profile: constrained-layout
    support: experimental
    spec: skin-fixtures/types/fixture-spec.md

  - id: fixture-topology
    selection_signal: "system components and boundaries"
    profile: constrained-layout
    support: experimental
    spec: skin-fixtures/types/fixture-spec.md
